<% if flash[:error] %>
  <% flash[:error].each do |r| %>
 <p> <%= r %> </p>
  <% end %>
<% end %>

<%= form_for @recipe do |f| %>

  <%= f.label :name%>
  <%= f.text_field :name%>
  <%= f.collection_select :user_id, User.all, :id, :name%>
<br>
<br>
  <%= f.submit %>
<% end %>